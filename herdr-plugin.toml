id = "herdr-simple-tab-names"
name = "Herdr Simple Tab Names"
version = "0.2.0"
min_herdr_version = "0.9.1"
description = "Name tabs after the foreground process-group leader"
platforms = ["linux", "macos"]

[[actions]]
id = "tick"
title = "Update the focused tab name"
contexts = ["global"]
command = ["bash", "rename-tab.sh"]
